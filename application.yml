#spring:
#  application:
#      name: Default-config 
#  profile:
#    active: dev
eureka:
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://localhost:8761/eureka
  instance:
    prefer-ip-address: true
